# project-clinic

clinic management system

nodejs, postgresql, sequelize dan docker

# BACKEND GUIDE

## github user guide

git clone <source>
git status
git add .
git commit -m "comments"
git push origin main

## npm installations

npm init -y
npm install express pg sequelize dotenv cors helmet express-rate-limit bcryptjs jsonwebtoken joi multer dayjs winston compression express-mongo-sanitize xss-clean hpp express-validator swagger-jsdoc swagger-ui-express redis node-cron --save
npm install nodemon jest supertest eslint eslint-config-airbnb-base eslint-plugin-import prettier sequelize-cli --save-dev
