# project-clinic

clinic management system

nodejs, postgresql, sequelize dan docker

# github user guide

git clone <source>
git status
git add .
git commit -m "comments"
git push origin main

npm init -y
npm install express cors pg dotenv sequelize --save
npm install nodemon --save-dev
